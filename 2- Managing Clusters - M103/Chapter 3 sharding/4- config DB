maintained internally by mongodb, don't write data to it 

it has all data about the cluster

it has several collections :
databases
collections
shards
chunks
locks
mongos
...

chunks:
  - each chunk of a collection is stored as a document
  - has min max key range relative to the associated collection

mongos:
  - holds data about the mongos processes connected to the cluster