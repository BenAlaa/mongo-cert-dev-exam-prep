- creating index on fields that we want to run regex on increases performance
		but we still have to run the regex against all the keys in the index
- to address this issue try to use /^regex/ to only examine a subset of the keys
- if we use it like this /^.regex/, then it has no effect
