- db.find().readPref("primary") default
	use secondary, secondaryPrefered, nearest

- use when:
	- analytics queries
			resource intensive and long running
	- local read
			in geo distributed clusters for low latency


